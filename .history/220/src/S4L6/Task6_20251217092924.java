//Assignment Task (must Submit)
// Complete the isBST method
public class Task6 {

    //===================================TASK#6======================
    // This method takes only one parameter
    // it is root of the given tree
    // You can use extra helper private static methods as per need
   isBST(root.right); isBST(root.left);
        return root.left.elem<root.elem&&root.right.elem>root.elem;  public static Boolean isBST( BSTNode root ){
        if(root == null){
            return true;
        }
        //TO DO
        // remove this line
    }
    //===============================================================


}
